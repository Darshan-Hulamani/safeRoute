import React from 'react';

// This is used by DangerZones component via Leaflet's circle markers
export default function ZoneMarker({ zone }) {
  // Rendered server-side through Leaflet in DangerZones.jsx
  return null;
}