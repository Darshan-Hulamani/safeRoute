// Uses react-hot-toast already configured globally.
import React from 'react';

// Using react-hot-toast which is already configured globally
// This component is a placeholder for custom toast UI if needed
export default function Toast({ message, type = 'success' }) {
  return null; // Using react-hot-toast's built-in toast
}